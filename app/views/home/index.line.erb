<%
url = "https://i.imgur.com/WWbqH9A.png"
%>
<%= raw(

  Kamiflex.json(self) do
    alt_text "hello world!"
    bubble do
      body borderColor: "#FF0000", borderWidth: :light do
        horizontal_box borderColor: "#00FF00", borderWidth: :light, justifyContent: "space-around" do
          image url, flex: 0
          image url, flex: 0
        end
      end
    end
  end
  
)%>